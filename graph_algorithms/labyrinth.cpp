#include <bits/stdc++.h> 

using namespace std;

int main() {
    int n{}, m{};
    cin >> n >> m;
    
    // 1. Take the input
    // 2. remember the start and end
    // 3. Used bfs to traverse and memorize the paths to the previous square
    // 4. If the end was reached, trace the path to the beginning. 
    
}